function Gd = gaussianDer(G,sigma)

    L=ceil(3*sigma);
    xVals = (-L:L);
    multiplTerm = -(xVals/(sigma^2));
    
    num = size(xVals);
    firstRes =
    
    
    Gd = multiplTerm * eye(num(2),num(2)) * G;


end